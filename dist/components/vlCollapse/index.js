import vlComponent from './vlCollapse'
import vlComponent2 from './vlCollapseItem'

export default Vue => {
  Vue.component(vlComponent.name, vlComponent)
  Vue.component(vlComponent2.name, vlComponent2)
}
